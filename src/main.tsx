import React from 'react';
import ReactDOM from 'react-dom/client';
import { AppOS } from './AppOS';
import './os-styles.css';
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><AppOS /></React.StrictMode>);
